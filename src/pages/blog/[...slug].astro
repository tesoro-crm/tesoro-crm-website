---
import Layout from '../../layouts/Layout.astro';
import RelatedPosts from '../../components/blog/RelatedPosts.astro';
import { getCollection } from 'astro:content';
import { getLocale } from '../../i18n/utils';
import type { Language } from '../../i18n/config';

export async function getStaticPaths() {
  const posts = await getCollection('blog');

  // Group by locale (fallback to default locale)
  const paths = posts
    .filter(post => !post.data.draft)
    .map(post => ({
      params: { slug: post.slug },
      props: { post },
    }));

  return paths;
}

const { post } = Astro.props;
const { Content, headings } = await post.render();
const locale: Language = getLocale(Astro.currentLocale, Astro.url);
---

<Layout
  title={`${post.data.title} | Tesoro CRM Blog`}
  description={post.data.description}
>
  <article class="blog-article">
    <div class="blog-article__container">
      <header class="blog-article__header">
        <div class="blog-article__meta">
          <span class="blog-article__category">{post.data.category}</span>
          <time class="blog-article__date" datetime={post.data.pubDate.toISOString()}>
            {post.data.pubDate.toLocaleDateString(locale === 'nl' ? 'nl-NL' : locale === 'es' ? 'es-ES' : 'en-US', {
              year: 'numeric',
              month: 'long',
              day: 'numeric'
            })}
          </time>
          {post.data.readingTime && (
            <span class="blog-article__reading-time">{post.data.readingTime} min read</span>
          )}
        </div>

        <h1 class="blog-article__title">{post.data.title}</h1>
        <p class="blog-article__description">{post.data.description}</p>

        {post.data.heroImage && (
          <div class="blog-article__hero">
            <img
              class="blog-article__hero-image"
              src={post.data.heroImage}
              alt={post.data.title}
              loading="eager"
              width="800"
              height="400"
            />
          </div>
        )}

        <div class="blog-article__author">
          {post.data.authorImage && (
            <img
              class="blog-article__author-avatar"
              src={post.data.authorImage}
              alt={post.data.author}
              loading="eager"
              width="48"
              height="48"
            />
          )}
          <div class="blog-article__author-info">
            <div class="blog-article__author-name">{post.data.author}</div>
            {post.data.authorBio && (
              <div class="blog-article__author-bio">{post.data.authorBio}</div>
            )}
          </div>
        </div>

        {post.data.aiGenerated && (
          <div class="blog-article__ai-badge" role="note" aria-label="AI generated content">
            🤖 AI-Generated Content
            {post.data.aiPrompt && (
              <details class="blog-article__ai-details">
                <summary>View AI Prompt</summary>
                <code>{post.data.aiPrompt}</code>
              </details>
            )}
          </div>
        )}
      </header>

      <div class="blog-article__content-wrapper">
        <main class="blog-article__main">
          <div class="blog-article__content">
            <Content />
          </div>
        </main>

        <aside class="blog-article__sidebar">
          <nav class="blog-article__toc" aria-labelledby="toc-heading">
            <h2 id="toc-heading" class="blog-article__toc-title">Inhoudsopgave</h2>
            <ol class="blog-article__toc-list">
              {headings
                .filter(heading => heading.depth <= 3)
                .map(heading => (
                  <li class={`blog-article__toc-item blog-article__toc-item--${heading.depth}`}>
                    <a href={`#${heading.slug}`} class="blog-article__toc-link">{heading.text}</a>
                  </li>
                ))}
            </ol>
          </nav>
        </aside>
      </div>

      <RelatedPosts currentPost={post} />

      <footer class="blog-article__footer">
        <div class="blog-article__tags" aria-label="Article tags">
          {post.data.tags.map(tag => (
            <span class="blog-article__tag">#{tag}</span>
          ))}
        </div>

        <div class="blog-article__share" aria-label="Share this article">
          <h3 class="blog-article__share-title">Deel dit artikel</h3>
          <div class="blog-article__share-buttons">
            <a
              href={`https://twitter.com/intent/tweet?text=${encodeURIComponent(post.data.title)}&url=${encodeURIComponent(Astro.url.href)}`}
              target="_blank"
              rel="noopener noreferrer"
              class="blog-article__share-button blog-article__share-button--twitter"
              aria-label="Deel op Twitter"
            >
              🐦 Twitter
            </a>
            <a
              href={`https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(Astro.url.href)}`}
              target="_blank"
              rel="noopener noreferrer"
              class="blog-article__share-button blog-article__share-button--linkedin"
              aria-label="Deel op LinkedIn"
            >
              💼 LinkedIn
            </a>
            <a
              href={`https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(Astro.url.href)}`}
              target="_blank"
              rel="noopener noreferrer"
              class="blog-article__share-button blog-article__share-button--facebook"
              aria-label="Deel op Facebook"
            >
              📘 Facebook
            </a>
            <button
              class="blog-article__share-button blog-article__share-button--copy"
              onclick={`navigator.clipboard.writeText('${Astro.url.href}')`}
              aria-label="Kopieer link"
            >
              📋 Kopieer Link
            </button>
          </div>
        </div>

        <div class="blog-article__newsletter" aria-labelledby="newsletter-heading">
          <h3 id="newsletter-heading" class="blog-article__newsletter-title">Blijf op de hoogte</h3>
          <p class="blog-article__newsletter-description">
            Krijg de laatste inzichten over CRM en vastgoedmarketing in je inbox.
          </p>
          <form class="blog-article__newsletter-form" data-newsletter-form aria-label="Nieuwsbrief aanmelden">
            <label for="newsletter-email" class="sr-only">Email adres</label>
            <input
              id="newsletter-email"
              type="email"
              placeholder="Jouw email adres"
              required
              class="blog-article__newsletter-input"
              aria-describedby="newsletter-description"
            />
            <button type="submit" class="blog-article__newsletter-submit">
              Abonneren
            </button>
          </form>
          <p id="newsletter-description" class="blog-article__newsletter-privacy">
            We respecteren je privacy. Afmelden kan altijd.
          </p>
        </div>
      </footer>
    </div>
  </article>
</Layout>

<script>
  // Newsletter signup
  document.addEventListener('DOMContentLoaded', () => {
    const form = document.querySelector('[data-newsletter-form]') as HTMLFormElement;
    if (!form) return;

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const emailInput = form.querySelector('input[type="email"]') as HTMLInputElement;
      const email = emailInput?.value;

      try {
        const response = await fetch('/api/newsletter', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ email }),
        });

        if (response.ok) {
          alert('Thanks for subscribing! Check your email.');
          form.reset();
        } else {
          alert('Something went wrong. Please try again.');
        }
      } catch (error) {
        alert('Something went wrong. Please try again.');
      }
    });
  });
</script>

<style>
  /* Base article layout */
  .blog-article {
    min-height: 100vh;
    background: white;
    padding: 3rem 0;
  }

  .blog-article__container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* Header styles */
  .blog-article__header {
    max-width: 900px;
    margin: 0 0 4rem 0;
  }

  .blog-article__meta {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
    font-size: 0.9rem;
    color: #64748b;
  }

  .blog-article__category {
    background: #0f172a;
    color: white;
    padding: 0.4rem 1rem;
    border-radius: 6px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-size: 0.75rem;
  }

  .blog-article__date {
    color: #64748b;
  }

  .blog-article__reading-time {
    color: #64748b;
  }

  .blog-article__title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 800;
    color: #0f172a;
    line-height: 1.15;
    margin-bottom: 1.5rem;
    letter-spacing: -0.03em;
  }

  .blog-article__description {
    font-size: 1.35rem;
    color: #475569;
    line-height: 1.65;
    margin-bottom: 3rem;
    font-weight: 400;
  }

  .blog-article__hero {
    margin-bottom: 3rem;
    border-radius: 12px;
    overflow: hidden;
    max-width: 900px;
  }

  .blog-article__hero-image {
    width: 100%;
    height: auto;
    display: block;
  }

  .blog-article__author {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.5rem 0;
    margin-bottom: 2rem;
    border-top: 1px solid #e2e8f0;
    border-bottom: 1px solid #e2e8f0;
  }

  .blog-article__author-avatar {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    object-fit: cover;
  }

  .blog-article__author-info {
    flex: 1;
  }

  .blog-article__author-name {
    font-size: 1rem;
    font-weight: 600;
    color: #0f172a;
    margin-bottom: 0.25rem;
  }

  .blog-article__author-bio {
    font-size: 0.875rem;
    color: #64748b;
    line-height: 1.5;
  }

  .blog-article__ai-badge {
    background: #f0f9ff;
    border: 1px solid #0ea5e9;
    color: #0ea5e9;
    padding: 1rem 1.5rem;
    border-radius: 8px;
    margin-bottom: 2rem;
    font-size: 0.875rem;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .blog-article__ai-details {
    margin-left: auto;
  }

  .blog-article__ai-details summary {
    cursor: pointer;
    font-size: 0.75rem;
    opacity: 0.8;
  }

  .blog-article__ai-details code {
    background: #1e293b;
    color: #f1f5f9;
    padding: 0.75rem;
    border-radius: 8px;
    font-size: 0.75rem;
    word-break: break-all;
    margin-top: 0.5rem;
    display: block;
  }

  /* Content wrapper - main content + sidebar */
  .blog-article__content-wrapper {
    display: grid;
    grid-template-columns: 1fr 280px;
    gap: 4rem;
    margin-bottom: 4rem;
    max-width: 1400px;
  }

  .blog-article__main {
    max-width: 750px;
    min-width: 0;
  }

  .blog-article__content {
    font-size: 1.125rem;
    line-height: 1.8;
    color: #1e293b;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
  }

  /* Typography improvements for readability */
  .blog-article__content h1,
  .blog-article__content h2,
  .blog-article__content h3,
  .blog-article__content h4,
  .blog-article__content h5,
  .blog-article__content h6 {
    color: #0f172a;
    margin-top: 3rem;
    margin-bottom: 1.25rem;
    line-height: 1.3;
    font-weight: 700;
    letter-spacing: -0.02em;
  }

  .blog-article__content h1 {
    font-size: 2.5rem;
    margin-top: 0;
  }

  .blog-article__content h2 {
    font-size: 2rem;
    margin-top: 3.5rem;
  }

  .blog-article__content h3 {
    font-size: 1.5rem;
  }

  .blog-article__content h4 {
    font-size: 1.25rem;
  }

  .blog-article__content p {
    margin-bottom: 1.5rem;
    line-height: 1.8;
  }

  .blog-article__content strong {
    font-weight: 600;
    color: #0f172a;
  }

  .blog-article__content ul,
  .blog-article__content ol {
    margin-bottom: 2rem;
    padding-left: 1.75rem;
  }

  .blog-article__content li {
    margin-bottom: 0.75rem;
    line-height: 1.75;
  }

  .blog-article__content li::marker {
    color: #64748b;
  }

  .blog-article__content blockquote {
    border-left: 3px solid #0f172a;
    padding-left: 1.5rem;
    margin: 2.5rem 0;
    font-style: italic;
    color: #475569;
    font-size: 1.15rem;
  }

  .blog-article__content code {
    background: #f1f5f9;
    padding: 0.2rem 0.4rem;
    border-radius: 4px;
    font-size: 0.9em;
    font-family: 'Monaco', 'Menlo', 'Ubuntu Mono', monospace;
    color: #0f172a;
  }

  .blog-article__content pre {
    background: #1e293b;
    color: #f1f5f9;
    padding: 1.5rem;
    border-radius: 8px;
    overflow-x: auto;
    margin: 2rem 0;
    font-size: 0.95rem;
  }

  .blog-article__content pre code {
    background: none;
    padding: 0;
    border-radius: 0;
    color: #f1f5f9;
  }

  .blog-article__content img {
    max-width: 100%;
    height: auto;
    border-radius: 8px;
    margin: 2.5rem 0;
  }

  .blog-article__content figcaption {
    text-align: center;
    font-size: 0.875rem;
    color: #64748b;
    margin-top: -1.5rem;
    margin-bottom: 2rem;
    font-style: italic;
  }

  .blog-article__content a {
    color: #0f172a;
    text-decoration: underline;
    text-decoration-color: #cbd5e1;
    text-underline-offset: 3px;
    transition: all 0.2s ease;
  }

  .blog-article__content a:hover {
    text-decoration-color: #0f172a;
  }

  /* Sidebar with Table of Contents */
  .blog-article__sidebar {
    position: sticky;
    top: 2rem;
    height: fit-content;
  }

  .blog-article__toc {
    background: #f8fafc;
    padding: 1.5rem;
    border-radius: 8px;
    border: 1px solid #e2e8f0;
  }

  .blog-article__toc-title {
    font-size: 0.875rem;
    font-weight: 700;
    color: #0f172a;
    margin-bottom: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .blog-article__toc-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .blog-article__toc-item {
    margin-bottom: 0.5rem;
    line-height: 1.4;
  }

  .blog-article__toc-item--1 {
    font-weight: 600;
    color: #0f172a;
    font-size: 0.875rem;
  }

  .blog-article__toc-item--2 {
    margin-left: 1rem;
    color: #475569;
    font-size: 0.8125rem;
  }

  .blog-article__toc-item--3 {
    margin-left: 2rem;
    color: #64748b;
    font-size: 0.8125rem;
  }

  .blog-article__toc-link {
    text-decoration: none;
    color: inherit;
    transition: color 0.2s ease;
    display: block;
    padding: 0.25rem 0;
  }

  .blog-article__toc-link:hover {
    color: #0f172a;
  }

  /* Footer */
  .blog-article__footer {
    max-width: 750px;
    padding: 3rem 0;
    border-top: 1px solid #e2e8f0;
    margin-top: 4rem;
  }

  .blog-article__tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 3rem;
  }

  .blog-article__tag {
    background: #f1f5f9;
    color: #475569;
    padding: 0.375rem 0.875rem;
    border-radius: 6px;
    font-size: 0.875rem;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .blog-article__tag:hover {
    background: #e2e8f0;
    color: #0f172a;
  }

  .blog-article__share {
    margin-bottom: 3rem;
  }

  .blog-article__share-title {
    font-size: 1.125rem;
    font-weight: 700;
    color: #0f172a;
    margin-bottom: 1rem;
  }

  .blog-article__share-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  .blog-article__share-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.875rem;
    transition: all 0.2s ease;
    border: 1px solid #e2e8f0;
  }

  .blog-article__share-button--twitter {
    background: #1da1f2;
    color: white;
    border-color: #1da1f2;
  }

  .blog-article__share-button--twitter:hover {
    background: #1a91da;
  }

  .blog-article__share-button--linkedin {
    background: #0077b5;
    color: white;
    border-color: #0077b5;
  }

  .blog-article__share-button--linkedin:hover {
    background: #005885;
  }

  .blog-article__share-button--facebook {
    background: #4267b2;
    color: white;
    border-color: #4267b2;
  }

  .blog-article__share-button--facebook:hover {
    background: #365899;
  }

  .blog-article__share-button--copy {
    background: white;
    color: #475569;
    border-color: #e2e8f0;
  }

  .blog-article__share-button--copy:hover {
    background: #f8fafc;
    color: #0f172a;
  }

  .blog-article__newsletter {
    background: #0f172a;
    color: white;
    padding: 2.5rem;
    border-radius: 8px;
  }

  .blog-article__newsletter-title {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
  }

  .blog-article__newsletter-description {
    margin-bottom: 1.5rem;
    opacity: 0.9;
    font-size: 1rem;
    line-height: 1.6;
  }

  .blog-article__newsletter-form {
    display: flex;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }

  .blog-article__newsletter-input {
    flex: 1;
    padding: 0.875rem 1rem;
    border: 1px solid #334155;
    border-radius: 6px;
    font-size: 1rem;
    background: #1e293b;
    color: white;
  }

  .blog-article__newsletter-input::placeholder {
    color: #94a3b8;
  }

  .blog-article__newsletter-input:focus {
    outline: 2px solid white;
    outline-offset: 2px;
    border-color: white;
  }

  .blog-article__newsletter-submit {
    background: white;
    color: #0f172a;
    border: none;
    padding: 0.875rem 1.5rem;
    border-radius: 6px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .blog-article__newsletter-submit:hover {
    background: #f1f5f9;
  }

  .blog-article__newsletter-privacy {
    font-size: 0.8125rem;
    opacity: 0.75;
  }

  /* Screen reader only content */
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  /* Focus management for accessibility */
  .blog-article__toc-link:focus,
  .blog-article__share-button:focus,
  .blog-article__newsletter-input:focus,
  .blog-article__newsletter-submit:focus {
    outline: 2px solid #667eea;
    outline-offset: 2px;
  }

  /* Skip link for screen readers */
  .skip-link {
    position: absolute;
    top: -40px;
    left: 6px;
    background: #667eea;
    color: white;
    padding: 8px;
    border-radius: 4px;
    text-decoration: none;
    z-index: 1000;
  }

  .skip-link:focus {
    top: 6px;
  }

  /* Responsive design */
  @media (max-width: 1200px) {
    .blog-article__content-wrapper {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .blog-article__sidebar {
      display: none;
    }

    .blog-article__main {
      max-width: 100%;
    }
  }

  @media (max-width: 768px) {
    .blog-article {
      padding: 2rem 0;
    }

    .blog-article__container {
      padding: 0 1.25rem;
    }

    .blog-article__header {
      margin-bottom: 3rem;
    }

    .blog-article__title {
      font-size: clamp(2rem, 8vw, 2.5rem);
    }

    .blog-article__description {
      font-size: 1.125rem;
    }

    .blog-article__content {
      font-size: 1.0625rem;
      line-height: 1.75;
    }

    .blog-article__content h2 {
      font-size: 1.75rem;
    }

    .blog-article__content h3 {
      font-size: 1.375rem;
    }

    .blog-article__author {
      padding: 1.25rem 0;
    }

    .blog-article__newsletter-form {
      flex-direction: column;
    }

    .blog-article__newsletter-input,
    .blog-article__newsletter-submit {
      width: 100%;
    }

    .blog-article__footer {
      padding: 2.5rem 0;
    }
  }

  @media (max-width: 640px) {
    .blog-article__meta {
      flex-wrap: wrap;
      gap: 0.75rem;
    }

    .blog-article__content blockquote {
      padding-left: 1.25rem;
      font-size: 1.0625rem;
    }
  }

  /* Print styles */
  @media print {
    .blog-article {
      background: white;
      padding: 0;
    }

    .blog-article__container {
      max-width: none;
      padding: 0;
    }

    .blog-article__sidebar,
    .blog-article__share,
    .blog-article__newsletter {
      display: none;
    }

    .blog-article__content {
      box-shadow: none;
      border: none;
      padding: 0;
    }
  }
</style>
