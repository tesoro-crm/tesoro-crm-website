---
import Layout from '../../layouts/Layout.astro';
import RelatedPosts from '../../components/blog/RelatedPosts.astro';
import SectionRenderer from '../../components/blog/SectionRenderer.astro';
import ImageWithFallback from '../../components/blog/ImageWithFallback.astro';
import { getCollection } from 'astro:content';
import { getLocale } from '../../i18n/utils';
import type { Language } from '../../types/blog';
import { formatDate, getSectionTitle, generateStructuredData, generateFAQStructuredData, getCanonicalUrl } from '../../utils/blogHelpers';
import { getAuthor } from '../../utils/authors';

export async function getStaticPaths() {
  const posts = await getCollection('blog');

  // Filter out drafts and create paths
  const paths = posts
    .filter(post => !post.data.meta.draft)
    .map(post => ({
      params: { slug: post.data.meta.id },
      props: { post },
    }));

  return paths;
}

const { post } = Astro.props;
const blogData = post.data;
const locale: Language = (getLocale(Astro.currentLocale, Astro.url) as Language) || 'nl';

// Get localized content
const title = blogData.hero.title[locale];
const description = blogData.hero.description[locale];
const heroImage = blogData.hero.image;
const authorId = (blogData.meta as any).authorId || (blogData.meta as any).author?.id || 'maria-perez';
const author = getAuthor(authorId);
if (!author) {
  throw new Error(`Author not found: ${authorId}`);
}
// Get publishDate from meta (new location) or hero (legacy fallback)
const publishDateString = (blogData.meta as any).publishDate || (blogData.hero as any).publishDate;
const publishDate = formatDate(publishDateString, locale);

// Generate TOC from sections
const tocItems = blogData.sections
  .filter(section => section.title || section.type === 'introduction')
  .map(section => ({
    id: section.id,
    title: getSectionTitle(section, locale)
  }));

// Generate canonical URL
const canonicalUrl = getCanonicalUrl(blogData.meta.id, Astro.url.origin);

// Generate structured data
const structuredData = generateStructuredData(blogData, locale, canonicalUrl);
const faqStructuredData = generateFAQStructuredData(blogData, locale);
---

<Layout
  title={`${title} | Tesoro CRM Blog`}
  description={description}
>
  <!-- Structured Data -->
  <script type="application/ld+json" set:html={JSON.stringify(structuredData)} />
  {faqStructuredData && (
    <script type="application/ld+json" set:html={JSON.stringify(faqStructuredData)} />
  )}

  <article class="blog-article">
    <div class="blog-article__container">
      <div class="grid grid-cols-1 lg:grid-cols-[1fr_320px] gap-8 lg:gap-12 items-start">
        <!-- Left column: Header + Content -->
        <div class="min-w-0">
          <header class="mb-12">
            <div class="blog-article__meta">
              <span class="blog-article__category">{blogData.meta.category}</span>
              <time class="blog-article__date" datetime={blogData.hero.publishDate}>
                {publishDate}
              </time>
              {blogData.meta.readingTime && (
                <span class="blog-article__reading-time">
                  {blogData.meta.readingTime} min {locale === 'nl' ? 'lezen' : locale === 'es' ? 'lectura' : 'read'}
                </span>
              )}
            </div>

            <h1 class="blog-article__title">{title}</h1>
            <p class="blog-article__description">{description}</p>

            <div class="blog-article__hero">
              <ImageWithFallback
                src={heroImage.src}
                alt={heroImage.alt[locale]}
                width={heroImage.width || 1200}
                height={heroImage.height || 600}
                loading="eager"
                class="blog-article__hero-image"
                aspectRatio="2/1"
              />
            </div>

            <div class="blog-article__author">
              <img
                src={author.image}
                alt={author.name}
                width={56}
                height={56}
                loading="eager"
                class="blog-article__author-avatar"
              />
              <div class="blog-article__author-info">
                <div class="blog-article__author-name">{author.name}</div>
                <div class="blog-article__author-bio">{author.bio[locale]}</div>
              </div>
            </div>

            {blogData.meta.aiGenerated && (
              <div class="blog-article__ai-badge" role="note" aria-label="AI generated content">
                🤖 {locale === 'nl' ? 'AI-Gegenereerde Content' : locale === 'es' ? 'Contenido Generado por IA' : 'AI-Generated Content'}
                {blogData.meta.aiPrompt && (
                  <details class="blog-article__ai-details">
                    <summary>{locale === 'nl' ? 'Bekijk AI Prompt' : locale === 'es' ? 'Ver Prompt de IA' : 'View AI Prompt'}</summary>
                    <code>{blogData.meta.aiPrompt}</code>
                  </details>
                )}
              </div>
            )}
          </header>

          <main>
            {blogData.sections.map(section => (
              <SectionRenderer section={section} lang={locale} />
            ))}
          </main>
        </div>

        <!-- Right column: TOC -->
        <aside class="lg:block hidden">
          <nav id="toc-nav" class="bg-slate-50 p-6 rounded-lg border border-slate-200 max-h-[calc(100vh-7rem)] overflow-y-auto" aria-labelledby="toc-heading">
            <h2 id="toc-heading" class="text-sm font-bold text-gray-900 mb-4 uppercase tracking-wider">
              {locale === 'nl' ? 'Inhoudsopgave' : locale === 'es' ? 'Tabla de Contenidos' : 'Table of Contents'}
            </h2>
            <ol class="space-y-2">
              {tocItems.map(item => (
                <li>
                  <a href={`#${item.id}`} data-toc-link={item.id} class="toc-link block py-1 text-sm text-gray-600 hover:text-gray-900 transition-colors">{item.title}</a>
                </li>
              ))}
            </ol>
          </nav>
        </aside>
      </div>

      <RelatedPosts currentPost={post} locale={locale} />

      <footer class="blog-article__footer">
        <div class="blog-article__tags" aria-label="Article tags">
          {blogData.meta.tags.map(tag => (
            <span class="blog-article__tag">#{tag}</span>
          ))}
        </div>

        <div class="blog-article__share" aria-label="Share this article">
          <h3 class="blog-article__share-title">
            {locale === 'nl' ? 'Deel dit artikel' : locale === 'es' ? 'Comparte este artículo' : 'Share this article'}
          </h3>
          <div class="blog-article__share-buttons">
            <a
              href={`https://twitter.com/intent/tweet?text=${encodeURIComponent(title)}&url=${encodeURIComponent(Astro.url.href)}`}
              target="_blank"
              rel="noopener noreferrer"
              class="blog-article__share-button blog-article__share-button--twitter"
              aria-label={locale === 'nl' ? 'Deel op Twitter' : locale === 'es' ? 'Compartir en Twitter' : 'Share on Twitter'}
            >
              🐦 Twitter
            </a>
            <a
              href={`https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(Astro.url.href)}`}
              target="_blank"
              rel="noopener noreferrer"
              class="blog-article__share-button blog-article__share-button--linkedin"
              aria-label={locale === 'nl' ? 'Deel op LinkedIn' : locale === 'es' ? 'Compartir en LinkedIn' : 'Share on LinkedIn'}
            >
              💼 LinkedIn
            </a>
            <a
              href={`https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(Astro.url.href)}`}
              target="_blank"
              rel="noopener noreferrer"
              class="blog-article__share-button blog-article__share-button--facebook"
              aria-label={locale === 'nl' ? 'Deel op Facebook' : locale === 'es' ? 'Compartir en Facebook' : 'Share on Facebook'}
            >
              📘 Facebook
            </a>
            <button
              class="blog-article__share-button blog-article__share-button--copy"
              onclick={`navigator.clipboard.writeText('${Astro.url.href}')`}
              aria-label={locale === 'nl' ? 'Kopieer link' : locale === 'es' ? 'Copiar enlace' : 'Copy link'}
            >
              📋 {locale === 'nl' ? 'Kopieer Link' : locale === 'es' ? 'Copiar Enlace' : 'Copy Link'}
            </button>
          </div>
        </div>

        <div class="blog-article__newsletter" aria-labelledby="newsletter-heading">
          <h3 id="newsletter-heading" class="blog-article__newsletter-title">
            {locale === 'nl' ? 'Blijf op de hoogte' : locale === 'es' ? 'Mantente informado' : 'Stay informed'}
          </h3>
          <p class="blog-article__newsletter-description">
            {locale === 'nl' 
              ? 'Krijg de laatste inzichten over CRM en vastgoedmarketing in je inbox.'
              : locale === 'es'
              ? 'Recibe las últimas perspectivas sobre CRM y marketing inmobiliario en tu bandeja de entrada.'
              : 'Get the latest insights on CRM and real estate marketing in your inbox.'}
          </p>
          <form class="blog-article__newsletter-form" data-newsletter-form aria-label={locale === 'nl' ? 'Nieuwsbrief aanmelden' : locale === 'es' ? 'Suscribirse al boletín' : 'Newsletter signup'}>
            <label for="newsletter-email" class="sr-only">
              {locale === 'nl' ? 'Email adres' : locale === 'es' ? 'Correo electrónico' : 'Email address'}
            </label>
            <input
              id="newsletter-email"
              type="email"
              placeholder={locale === 'nl' ? 'Jouw email adres' : locale === 'es' ? 'Tu correo electrónico' : 'Your email address'}
              required
              class="blog-article__newsletter-input"
              aria-describedby="newsletter-description"
            />
            <button type="submit" class="blog-article__newsletter-submit">
              {locale === 'nl' ? 'Abonneren' : locale === 'es' ? 'Suscribirse' : 'Subscribe'}
            </button>
          </form>
          <p id="newsletter-description" class="blog-article__newsletter-privacy">
            {locale === 'nl' 
              ? 'We respecteren je privacy. Afmelden kan altijd.'
              : locale === 'es'
              ? 'Respetamos tu privacidad. Puedes darte de baja en cualquier momento.'
              : 'We respect your privacy. Unsubscribe anytime.'}
          </p>
        </div>
      </footer>
    </div>
  </article>
</Layout>

<script>
  // Newsletter signup
  document.addEventListener('DOMContentLoaded', () => {
    const form = document.querySelector('[data-newsletter-form]') as HTMLFormElement;
    if (!form) return;

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const emailInput = form.querySelector('input[type="email"]') as HTMLInputElement;
      const email = emailInput?.value;

      try {
        const response = await fetch('/api/newsletter', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ email }),
        });

        if (response.ok) {
          alert('Thanks for subscribing! Check your email.');
          form.reset();
        } else {
          alert('Something went wrong. Please try again.');
        }
      } catch (error) {
        alert('Something went wrong. Please try again.');
      }
    });
  });

  // Sticky TOC behavior
  const tocNav = document.getElementById('toc-nav');
  if (tocNav) {
    const navHeight = 80; // Height of main navigation
    const stickyOffset = navHeight + 24; // 24px gap
    
    let tocOriginalTop = 0;
    let tocOriginalLeft = 0;
    
    // Calculate position after everything is loaded
    const calculatePosition = () => {
      const rect = tocNav.getBoundingClientRect();
      tocOriginalTop = rect.top + window.scrollY;
      tocOriginalLeft = rect.left;
    };
    
    // Wait for images and layout to settle
    window.addEventListener('load', () => {
      setTimeout(calculatePosition, 100);
    });
    
    // Recalculate on resize
    window.addEventListener('resize', calculatePosition);
    
    window.addEventListener('scroll', () => {
      if (tocOriginalTop === 0) return; // Not initialized yet
      
      const scrollTop = window.scrollY;
      
      if (scrollTop + stickyOffset >= tocOriginalTop) {
        // Make it sticky
        tocNav.style.position = 'fixed';
        tocNav.style.top = `${stickyOffset}px`;
        tocNav.style.left = `${tocOriginalLeft}px`;
        tocNav.style.width = '280px';
      } else {
        // Reset to normal flow
        tocNav.style.position = 'static';
        tocNav.style.top = 'auto';
        tocNav.style.left = 'auto';
        tocNav.style.width = 'auto';
      }
    }, { passive: true });
  }

  // Active section highlighting in TOC
  const observerOptions = {
    rootMargin: '-100px 0px -66%',
    threshold: 0
  };

  const observerCallback = (entries: IntersectionObserverEntry[]) => {
    entries.forEach((entry: IntersectionObserverEntry) => {
      const id = entry.target.getAttribute('id');
      const tocLink = document.querySelector(`a[data-toc-link="${id}"]`);
      
      if (tocLink) {
        if (entry.isIntersecting) {
          // Remove active from all links
          document.querySelectorAll('.toc-link').forEach(link => {
            link.classList.remove('!text-blue-600', '!font-semibold', 'border-l-2', 'border-blue-600', 'pl-2', '-ml-2');
          });
          
          // Add active to current link
          tocLink.classList.add('!text-blue-600', '!font-semibold', 'border-l-2', 'border-blue-600', 'pl-2', '-ml-2');
        }
      }
    });
  };

  const observer = new IntersectionObserver(observerCallback, observerOptions);

  // Observe all sections
  document.querySelectorAll('section[id]').forEach(section => {
    observer.observe(section);
  });
</script>

<style>
  /* Base article layout */
  .blog-article {
    min-height: 100vh;
    background: white;
    padding: 3rem 0;
  }

  .blog-article__container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* Header styles */
  .blog-article__header {
    margin: 0 0 4rem 0;
  }

  .blog-article__meta {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
    font-size: 0.9rem;
    color: #64748b;
  }

  .blog-article__category {
    font-family: var(--font-body);
    background: var(--bg-dark);
    color: white;
    padding: 0.4rem 1rem;
    border-radius: 9999px;
    font-size: 0.875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .blog-article__date {
    color: #64748b;
  }

  .blog-article__reading-time {
    color: #64748b;
  }

  .blog-article__title {
    font-family: var(--font-heading);
    font-size: clamp(2rem, 4vw, 2.5rem);
    font-weight: 500;
    color: var(--text-primary);
    line-height: 1.2;
    margin-bottom: 1.5rem;
    letter-spacing: -0.02em;
  }

  .blog-article__description {
    font-family: var(--font-body);
    font-size: 1.35rem;
    color: var(--text-body);
    line-height: 1.65;
    margin-bottom: 2rem;
    font-weight: 500;
  }

  .blog-article__hero {
    margin-bottom: 3rem;
    border-radius: 12px;
    overflow: hidden;
  }

  .blog-article__hero-image {
    width: 100%;
    height: auto;
    display: block;
  }

  .blog-article__author {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.5rem 0;
    margin-bottom: 2rem;
    border-top: 1px solid #e2e8f0;
    border-bottom: 1px solid #e2e8f0;
  }

  .blog-article__author-avatar {
    width: 56px !important;
    height: 56px !important;
    min-width: 56px;
    min-height: 56px;
    max-width: 56px;
    max-height: 56px;
    border-radius: 50%;
    object-fit: cover;
    flex-shrink: 0;
  }

  .blog-article__author-info {
    flex: 1;
  }

  .blog-article__author-name {
    font-size: 1rem;
    font-weight: 600;
    color: #0f172a;
    margin-bottom: 0.25rem;
  }

  .blog-article__author-bio {
    font-size: 0.875rem;
    color: #64748b;
    line-height: 1.5;
  }

  .blog-article__ai-badge {
    background: #f0f9ff;
    border: 1px solid #0ea5e9;
    color: #0ea5e9;
    padding: 1rem 1.5rem;
    border-radius: 8px;
    margin-bottom: 2rem;
    font-size: 0.875rem;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .blog-article__ai-details {
    margin-left: auto;
  }

  .blog-article__ai-details summary {
    cursor: pointer;
    font-size: 0.75rem;
    opacity: 0.8;
  }

  .blog-article__ai-details code {
    background: #1e293b;
    color: #f1f5f9;
    padding: 0.75rem;
    border-radius: 8px;
    font-size: 0.75rem;
    word-break: break-all;
    margin-top: 0.5rem;
    display: block;
  }

  /* Content wrapper - main content + sidebar */
  .blog-article__content-wrapper {
    display: grid;
    grid-template-columns: 1fr 280px;
    gap: 4rem;
    margin-bottom: 4rem;
    max-width: 1400px;
    align-items: start;
  }

  .blog-article__main {
    min-width: 0;
  }

  .blog-article__content {
    font-size: 1.125rem;
    line-height: 1.8;
    color: var(--text-secondary);
    font-family: var(--font-body);
    font-weight: 400;
  }

  /* Typography improvements for readability */
  .blog-article__content h1 {
    font-family: var(--font-heading);
    color: var(--text-primary);
    margin-top: 3rem;
    margin-bottom: 1.25rem;
    line-height: 1.3;
    font-weight: 500;
    letter-spacing: -0.02em;
  }

  .blog-article__content h2,
  .blog-article__content h3 {
    font-family: var(--font-decorative);
    color: var(--text-primary);
    margin-top: 3rem;
    margin-bottom: 1.25rem;
    line-height: 1.3;
    font-weight: 700;
    letter-spacing: -0.01em;
    text-transform: none;
  }

  .blog-article__content h4,
  .blog-article__content h5,
  .blog-article__content h6 {
    font-family: var(--font-heading);
    color: var(--text-primary);
    margin-top: 3rem;
    margin-bottom: 1.25rem;
    line-height: 1.3;
    font-weight: 600;
    letter-spacing: -0.02em;
  }

  .blog-article__content h1 {
    font-size: 2.5rem;
    margin-top: 0;
  }

  .blog-article__content h2 {
    font-size: 1.75rem;
    margin-top: 3.5rem;
  }

  .blog-article__content h3 {
    font-size: 1.5rem;
  }

  .blog-article__content h4 {
    font-size: 1.25rem;
  }

  .blog-article__content p {
    margin-bottom: 1.5rem;
    line-height: 1.8;
  }

  .blog-article__content strong {
    font-weight: 600;
    color: #0f172a;
  }

  .blog-article__content ul,
  .blog-article__content ol {
    margin-bottom: 2rem;
    padding-left: 1.75rem;
  }

  .blog-article__content li {
    margin-bottom: 0.75rem;
    line-height: 1.75;
  }

  .blog-article__content li::marker {
    color: #64748b;
  }

  .blog-article__content blockquote {
    border-left: 3px solid #0f172a;
    padding-left: 1.5rem;
    margin: 2.5rem 0;
    font-style: italic;
    color: #475569;
    font-size: 1.15rem;
  }

  .blog-article__content code {
    background: #f1f5f9;
    padding: 0.2rem 0.4rem;
    border-radius: 4px;
    font-size: 0.9em;
    font-family: var(--font-mono);
    color: var(--text-primary);
  }

  .blog-article__content pre {
    background: #1e293b;
    color: #f1f5f9;
    padding: 1.5rem;
    border-radius: 8px;
    overflow-x: auto;
    margin: 2rem 0;
    font-size: 0.95rem;
  }

  .blog-article__content pre code {
    background: none;
    padding: 0;
    border-radius: 0;
    color: #f1f5f9;
  }

  .blog-article__content img:not(.blog-article__author-avatar),
  .blog-article__content .image-with-fallback {
    width: 100%;
    max-width: 100%;
    height: auto;
    border-radius: 8px;
    margin: 2.5rem 0;
  }
  
  /* Ensure author avatar is never affected by content styles */
  img.blog-article__author-avatar {
    width: 56px !important;
    height: 56px !important;
    border-radius: 50% !important;
    margin: 0 !important;
  }

  .blog-article__content figcaption {
    text-align: center;
    font-size: 0.875rem;
    color: #64748b;
    margin-top: -1.5rem;
    margin-bottom: 2rem;
    font-style: italic;
  }

  .blog-article__content a {
    color: #0f172a;
    text-decoration: underline;
    text-decoration-color: #cbd5e1;
    text-underline-offset: 3px;
    transition: all 0.2s ease;
  }

  .blog-article__content a:hover {
    text-decoration-color: #0f172a;
  }

  /* Sidebar with Table of Contents */
  .blog-article__sidebar {
    min-width: 280px;
    max-width: 320px;
  }

  .blog-article__toc {
    position: -webkit-sticky;
    position: sticky;
    top: 100px;
    background: #f8fafc;
    padding: 1.5rem;
    border-radius: 8px;
    border: 1px solid #e2e8f0;
    max-height: calc(100vh - 120px);
    overflow-y: auto;
    align-self: start;
  }

  .blog-article__toc-title {
    font-size: 0.875rem;
    font-weight: 700;
    color: #0f172a;
    margin-bottom: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .blog-article__toc-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .blog-article__toc-item {
    margin-bottom: 0.5rem;
    line-height: 1.4;
  }

  .blog-article__toc-item--1 {
    font-weight: 600;
    color: #0f172a;
    font-size: 0.875rem;
  }

  .blog-article__toc-item--2 {
    margin-left: 1rem;
    color: #475569;
    font-size: 0.8125rem;
  }

  .blog-article__toc-item--3 {
    margin-left: 2rem;
    color: #64748b;
    font-size: 0.8125rem;
  }

  .blog-article__toc-link {
    text-decoration: none;
    color: inherit;
    transition: color 0.2s ease;
    display: block;
    padding: 0.25rem 0;
  }

  .blog-article__toc-link:hover {
    color: #0f172a;
  }

  /* Footer */
  .blog-article__footer {
    padding: 3rem 0;
    border-top: 1px solid #e2e8f0;
    margin-top: 4rem;
  }

  .blog-article__tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 3rem;
  }

  .blog-article__tag {
    background: #f1f5f9;
    color: #475569;
    padding: 0.375rem 0.875rem;
    border-radius: 6px;
    font-size: 0.875rem;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .blog-article__tag:hover {
    background: #e2e8f0;
    color: #0f172a;
  }

  .blog-article__share {
    margin-bottom: 3rem;
  }

  .blog-article__share-title {
    font-size: 1.125rem;
    font-weight: 700;
    color: #0f172a;
    margin-bottom: 1rem;
  }

  .blog-article__share-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  .blog-article__share-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.875rem;
    transition: all 0.2s ease;
    border: 1px solid #e2e8f0;
  }

  .blog-article__share-button--twitter {
    background: #1da1f2;
    color: white;
    border-color: #1da1f2;
  }

  .blog-article__share-button--twitter:hover {
    background: #1a91da;
  }

  .blog-article__share-button--linkedin {
    background: #0077b5;
    color: white;
    border-color: #0077b5;
  }

  .blog-article__share-button--linkedin:hover {
    background: #005885;
  }

  .blog-article__share-button--facebook {
    background: #4267b2;
    color: white;
    border-color: #4267b2;
  }

  .blog-article__share-button--facebook:hover {
    background: #365899;
  }

  .blog-article__share-button--copy {
    background: white;
    color: #475569;
    border-color: #e2e8f0;
  }

  .blog-article__share-button--copy:hover {
    background: #f8fafc;
    color: #0f172a;
  }

  .blog-article__newsletter {
    background: #0f172a;
    color: white;
    padding: 2.5rem;
    border-radius: 8px;
  }

  .blog-article__newsletter-title {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
  }

  .blog-article__newsletter-description {
    margin-bottom: 1.5rem;
    opacity: 0.9;
    font-size: 1rem;
    line-height: 1.6;
  }

  .blog-article__newsletter-form {
    display: flex;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }

  .blog-article__newsletter-input {
    flex: 1;
    padding: 0.875rem 1rem;
    border: 1px solid #334155;
    border-radius: 6px;
    font-size: 1rem;
    background: #1e293b;
    color: white;
  }

  .blog-article__newsletter-input::placeholder {
    color: #94a3b8;
  }

  .blog-article__newsletter-input:focus {
    outline: 2px solid white;
    outline-offset: 2px;
    border-color: white;
  }

  .blog-article__newsletter-submit {
    background: white;
    color: #0f172a;
    border: none;
    padding: 0.875rem 1.5rem;
    border-radius: 6px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .blog-article__newsletter-submit:hover {
    background: #f1f5f9;
  }

  .blog-article__newsletter-privacy {
    font-size: 0.8125rem;
    opacity: 0.75;
  }

  /* Screen reader only content */
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  /* Focus management for accessibility */
  .blog-article__toc-link:focus,
  .blog-article__share-button:focus,
  .blog-article__newsletter-input:focus,
  .blog-article__newsletter-submit:focus {
    outline: 2px solid #667eea;
    outline-offset: 2px;
  }

  /* Skip link for screen readers */
  .skip-link {
    position: absolute;
    top: -40px;
    left: 6px;
    background: #667eea;
    color: white;
    padding: 8px;
    border-radius: 4px;
    text-decoration: none;
    z-index: 1000;
  }

  .skip-link:focus {
    top: 6px;
  }

  /* Responsive design */
  @media (max-width: 1200px) {
    .blog-article__content-wrapper {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .blog-article__sidebar {
      display: none;
    }

    .blog-article__main {
      /* No max-width needed - grid handles it */
    }
  }

  @media (max-width: 768px) {
    .blog-article {
      padding: 2rem 0;
    }

    .blog-article__container {
      padding: 0 1.25rem;
    }

    .blog-article__header {
      margin-bottom: 3rem;
    }

    .blog-article__title {
      font-size: clamp(2rem, 8vw, 2.5rem);
    }

    .blog-article__description {
      font-size: 1.125rem;
    }

    .blog-article__content {
      font-size: 1.0625rem;
      line-height: 1.75;
    }

    .blog-article__content h2 {
      font-size: 1.75rem;
    }

    .blog-article__content h3 {
      font-size: 1.375rem;
    }

    .blog-article__author {
      padding: 1.25rem 0;
    }

    .blog-article__newsletter-form {
      flex-direction: column;
    }

    .blog-article__newsletter-input,
    .blog-article__newsletter-submit {
      width: 100%;
    }

    .blog-article__footer {
      padding: 2.5rem 0;
    }
  }

  @media (max-width: 640px) {
    .blog-article__meta {
      flex-wrap: wrap;
      gap: 0.75rem;
    }

    .blog-article__content blockquote {
      padding-left: 1.25rem;
      font-size: 1.0625rem;
    }
  }

  /* Print styles */
  @media print {
    .blog-article {
      background: white;
      padding: 0;
    }

    .blog-article__container {
      max-width: none;
      padding: 0;
    }

    .blog-article__sidebar,
    .blog-article__share,
    .blog-article__newsletter {
      display: none;
    }

    .blog-article__content {
      box-shadow: none;
      border: none;
      padding: 0;
    }
  }
</style>
